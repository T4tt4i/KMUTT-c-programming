#include<stdio.h>
#include<math.h>

int main()
{
    double a;
    double b;
    double c2;
    double result;

scanf("%lf%lf", &a, &b);
c2=a*a+b*b;
result = sqrt(c2);
printf("%lf",result);
return 0;

 

}